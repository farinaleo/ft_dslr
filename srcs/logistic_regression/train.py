import numpy as np
import pandas as pd
from gradient_descent import gradient_descent


def train_model(df: pd.DataFrame, y_col: str, learning_rate: float = 0.1, epoch: int = 1000) -> dict:
    model = {}

    return model
